---
import Layout from '~/layouts/PageLayout.astro';
import { SITE } from '~/config.mjs';
const meta = {
  title: SITE.title,
  description: SITE.description,
  dontUseTitleTemplate: true,
};

const {

} = Astro.props;
---
<Layout {meta}>
    <div class="px-3 mx-auto mt-16 mb-16 max-w-7xl">  
        <slot />    
    <br>
</Layout>