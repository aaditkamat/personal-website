---
const {
    link = "",
    title = "",
    info = "",
} = Astro.props;
---
<div class="border-2 border-black rounded-xl p-10 h-fit">
    <i class="fab fa-github fa-lg inline-block align-middle" /> <a href={link} class="font-bold text-l text-left inline-block align-middle">{title}</span>

    <span>{info}</span>
</div>